<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Posizione. Il GPS non richiede rete: e' il senso dell'app.
         Nessun permesso di posizione in background e nessun servizio in primo
         piano: la posizione si registra solo mentre l'app e' aperta, perche'
         la traccia continua e' fuori scope per scelta. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- La rete serve solo per fare scorta in anticipo: meteo la sera,
         distanze su strada quando si importa un itinerario. Nessuna
         schermata la aspetta, e ogni lettura funziona senza. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Il riepilogo della sera. Da Android 13 il permesso si chiede a
         runtime, e senza di esso la notifica viene scartata in silenzio. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Un riavvio cancella tutte le sveglie: senza questo, il riepilogo
         smetterebbe di arrivare senza dire niente.
         Nessun SCHEDULE_EXACT_ALARM: un riepilogo serale tollera qualche
         minuto di scarto, e quel permesso e' riservato alle sveglie. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Niente CAMERA: le foto si scattano con l'app fotocamera di sistema,
         a cui si passa il file da scrivere. Chiedere il permesso servirebbe
         solo per usare la fotocamera da dentro l'app, che non ci serve. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />


    <application
        android:name=".MyaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyaCamperLife">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyaCamperLife"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Un itinerario condiviso da un'altra app. Solo text/*: un
                 filtro su */* farebbe comparire l'app nella condivisione di
                 qualunque cosa, e non e' un buon vicino di casa. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>

        </activity>

        <!-- Le 19:00. Non esportato: lo sveglia solo il nostro AlarmManager. -->
        <receiver
            android:name=".avvisi.BriefingReceiver"
            android:exported="false" />

        <!-- Riarma la sveglia dopo un riavvio o un aggiornamento dell'app.
             Esportato perche' e' il sistema a mandare questi eventi. -->
        <receiver
            android:name=".avvisi.AvvioReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Passa alla fotocamera di sistema il file dove scrivere lo scatto.
             Non esportato: i permessi si concedono un Uri per volta. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.file"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
